require "test_helper"

class UserTest < ActiveSupport::TestCase
  test "New reader can be created" do
    assert true
  end

  test "New admin can be created" do
    assert true
  end

  test "User details can be updated" do
    assert true
  end

  test "User can authenticate" do
    assert true
  end

  test "User can be deleted" do
    assert true
  end

  test "User must have valid name" do
    assert true
  end

  test "User must have valid email" do
    assert true
  end

  test "User email must be unique" do
    assert true
  end

  test "User can be assigned a role" do
    assert true
  end

  test "User must have at least one role" do
    assert true
  end
end
