require "test_helper"

class RoleTest < ActiveSupport::TestCase
  test "A new role can be created" do
    assert true
  end

  test "Roles can be updated" do
    assert true
  end

  test "Roles can be deleted" do
    assert true
  end

  test "Roles must have at least one permission" do
    assert true
  end

  test "Roles automatically upgrade permissions" do
    assert true
  end

  test "Roles can be queried for capabilities" do
    assert true
  end
end
